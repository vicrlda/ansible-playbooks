------------------------------------------------
                Servidor Web

Hostname: "{{ ansible_fqdn }}"
Endereco_IP: "{{ ansible_all_ipv4_addresses }}"

------------------------------------------------
